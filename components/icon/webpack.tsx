import WebpackIcon from '@iconify-react/logos/webpack';
import { ComponentProps } from 'react';

export const Webpack = (props: ComponentProps<typeof WebpackIcon>) => (
  <WebpackIcon {...props} />
);
