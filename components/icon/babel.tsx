import { Babel as BabelIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Babel = (props: ComponentProps<typeof BabelIcon>) => <BabelIcon {...props} />

export { Babel }
