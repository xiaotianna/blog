import { Yarn as YarnIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Yarn = (props: ComponentProps<typeof YarnIcon>) => <YarnIcon {...props} />

export { Yarn }
