import { GraphQL as GraphQLIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const GraphQL = (props: ComponentProps<typeof GraphQLIcon>) => <GraphQLIcon {...props} />

export { GraphQL }
