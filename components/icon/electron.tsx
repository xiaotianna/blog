import { Electron as ElectronIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Electron = (props: ComponentProps<typeof ElectronIcon>) => <ElectronIcon {...props} />

export { Electron }
