import { NestJS as NestIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Nest = (props: ComponentProps<typeof NestIcon>) => <NestIcon {...props} />

export { Nest }
