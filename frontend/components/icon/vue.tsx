import { Vue as VueIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Vue = (props: ComponentProps<typeof VueIcon>) => <VueIcon {...props} />

export { Vue }
