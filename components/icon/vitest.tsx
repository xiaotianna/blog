import { Vitest as VitestIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Vitest = (props: ComponentProps<typeof VitestIcon>) => <VitestIcon {...props} />

export { Vitest }
