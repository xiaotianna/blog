import { TypeORM as TypeORMIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const TypeORM = (props: ComponentProps<typeof TypeORMIcon>) => <TypeORMIcon {...props} />

export { TypeORM }
