import { Dotenvx as EnvIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Env = (props: ComponentProps<typeof EnvIcon>) => <EnvIcon {...props} />

export { Env }
