import { Vite as ViteIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Vite = (props: ComponentProps<typeof ViteIcon>) => <ViteIcon {...props} />

export { Vite }
