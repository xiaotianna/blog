import { Nodejs as NodeIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Node = (props: ComponentProps<typeof NodeIcon>) => <NodeIcon {...props} />

export { Node }
