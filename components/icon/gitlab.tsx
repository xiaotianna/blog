import { GitLab as GitLabIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const GitLab = (props: ComponentProps<typeof GitLabIcon>) => <GitLabIcon {...props} />

export { GitLab }
