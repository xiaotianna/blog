import { Sass as SassIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Sass = (props: ComponentProps<typeof SassIcon>) => <SassIcon {...props} />

export { Sass }
