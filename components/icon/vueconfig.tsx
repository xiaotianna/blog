import { Vue as VueConfigIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const VueConfig = (props: ComponentProps<typeof VueConfigIcon>) => <VueConfigIcon {...props} />

export { VueConfig }
