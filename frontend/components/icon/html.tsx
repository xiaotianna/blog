import { HTML5 as HTML5Icon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const HTML5 = (props: ComponentProps<typeof HTML5Icon>) => <HTML5Icon {...props} />

export { HTML5 }
