import LessIcon from '@iconify-react/material-icon-theme/less';
import { ComponentProps } from 'react';

export const Less = (props: ComponentProps<typeof LessIcon>) => (
  <LessIcon {...props} />
);
