import { UnoCSS as UnoCSSIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const UnoCSS = (props: ComponentProps<typeof UnoCSSIcon>) => <UnoCSSIcon {...props} />

export { UnoCSS }
