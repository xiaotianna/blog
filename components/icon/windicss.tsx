import { WindiCSS as WindiCSSIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const WindiCSS = (props: ComponentProps<typeof WindiCSSIcon>) => <WindiCSSIcon {...props} />

export { WindiCSS }
