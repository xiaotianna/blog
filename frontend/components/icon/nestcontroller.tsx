import { NestJS as NestControllerIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const NestController = (props: ComponentProps<typeof NestControllerIcon>) => (
  <NestControllerIcon {...props} />
)

export { NestController }
