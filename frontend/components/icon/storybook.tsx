import { Storybook as StorybookIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Storybook = (props: ComponentProps<typeof StorybookIcon>) => <StorybookIcon {...props} />

export { Storybook }
