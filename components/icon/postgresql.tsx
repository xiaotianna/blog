import { PostgreSQL as PostgreSQLIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const PostgreSQL = (props: ComponentProps<typeof PostgreSQLIcon>) => (
  <PostgreSQLIcon {...props} />
)

export { PostgreSQL }
