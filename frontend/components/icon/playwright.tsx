import { Playwright as PlaywrightIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Playwright = (props: ComponentProps<typeof PlaywrightIcon>) => (
  <PlaywrightIcon {...props} />
)

export { Playwright }
