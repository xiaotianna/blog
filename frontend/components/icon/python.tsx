import { Python as PythonIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Python = (props: ComponentProps<typeof PythonIcon>) => <PythonIcon {...props} />

export { Python }
