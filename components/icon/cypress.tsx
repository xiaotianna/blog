import { Cypress as CypressIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Cypress = (props: ComponentProps<typeof CypressIcon>) => <CypressIcon {...props} />

export { Cypress }
