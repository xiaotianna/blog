import { Svelte as SvelteIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Svelte = (props: ComponentProps<typeof SvelteIcon>) => <SvelteIcon {...props} />

export { Svelte }
