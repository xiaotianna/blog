import { Nginx as NginxIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Nginx = (props: ComponentProps<typeof NginxIcon>) => <NginxIcon {...props} />

export { Nginx }
