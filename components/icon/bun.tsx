import { Bun as BunIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Bun = (props: ComponentProps<typeof BunIcon>) => <BunIcon {...props} />

export { Bun }
