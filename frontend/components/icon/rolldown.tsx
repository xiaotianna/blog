import { Rolldown as RolldownIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Rolldown = (props: ComponentProps<typeof RolldownIcon>) => <RolldownIcon {...props} />

export { Rolldown }
