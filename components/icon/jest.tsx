import { Jest as JestIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Jest = (props: ComponentProps<typeof JestIcon>) => <JestIcon {...props} />

export { Jest }
