import FileTypeSqlIcon from '@iconify-react/vscode-icons/file-type-sql';
import { ComponentProps } from 'react';

export const Sql = (props: ComponentProps<typeof FileTypeSqlIcon>) => (
  <FileTypeSqlIcon {...props} />
);
