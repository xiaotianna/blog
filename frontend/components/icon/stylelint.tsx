import FileTypeLightStylelintIcon from '@iconify-react/vscode-icons/file-type-light-stylelint';
import { ComponentProps } from 'react';

export const Stylelint = (props: ComponentProps<typeof FileTypeLightStylelintIcon>) => (
  <FileTypeLightStylelintIcon {...props} />
);
