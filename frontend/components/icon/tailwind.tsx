import { TailwindCSS as TailwindIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Tailwind = (props: ComponentProps<typeof TailwindIcon>) => <TailwindIcon {...props} />

export { Tailwind }
