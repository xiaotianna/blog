import { CSS as CSSIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const CSS = (props: ComponentProps<typeof CSSIcon>) => <CSSIcon {...props} />

export { CSS }
