import { Docusaurus as DocusaurusIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Docusaurus = (props: ComponentProps<typeof DocusaurusIcon>) => (
  <DocusaurusIcon {...props} />
)

export { Docusaurus }
