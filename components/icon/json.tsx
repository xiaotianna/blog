import { JSON as JSONIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const JSON = (props: ComponentProps<typeof JSONIcon>) => <JSONIcon {...props} />

export { JSON }
