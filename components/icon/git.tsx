import { Git as GitIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Git = (props: ComponentProps<typeof GitIcon>) => <GitIcon {...props} />

export { Git }
