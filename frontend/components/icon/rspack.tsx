import { Rspack as RspackIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Rspack = (props: ComponentProps<typeof RspackIcon>) => <RspackIcon {...props} />

export { Rspack }
