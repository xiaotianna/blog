import { TypeScript as TypeScriptIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const TypeScript = (props: ComponentProps<typeof TypeScriptIcon>) => (
  <TypeScriptIcon {...props} />
)

export { TypeScript }
