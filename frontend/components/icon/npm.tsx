import { NPM as NPMIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const NPM = (props: ComponentProps<typeof NPMIcon>) => <NPMIcon {...props} />

export { NPM }
