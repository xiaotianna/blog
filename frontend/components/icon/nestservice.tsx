import { NestJS as NestServiceIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const NestService = (props: ComponentProps<typeof NestServiceIcon>) => (
  <NestServiceIcon {...props} />
)

export { NestService }
