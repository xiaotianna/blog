import { Rsbuild as RsbuildIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Rsbuild = (props: ComponentProps<typeof RsbuildIcon>) => <RsbuildIcon {...props} />

export { Rsbuild }
