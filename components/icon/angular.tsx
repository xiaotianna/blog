import { Angular as AngularIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Angular = (props: ComponentProps<typeof AngularIcon>) => <AngularIcon {...props} />

export { Angular }
