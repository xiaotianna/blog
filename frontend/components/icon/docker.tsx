import { Docker as DockerIcon } from '@ridemountainpig/svgl-react'
import type { ComponentProps } from 'react'

const Docker = (props: ComponentProps<typeof DockerIcon>) => <DockerIcon {...props} />

export { Docker }
